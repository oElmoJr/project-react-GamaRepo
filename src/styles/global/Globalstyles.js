import { createGlobalStyle } from "styled-components";

export const GlobalStyle = createGlobalStyle`

    * {
        margin: 0;
        padding: 0;
        font-family: 'Cormorant Garamond', serif;
    }

    body {
        background-color: #CA8968;
    }

    html, body {
        height: 100vh;
    }
`;
